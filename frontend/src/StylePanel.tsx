import { useEditor } from './editor-store'
import { ResumeStyle } from './resume-types'

const presets=[{name:'Conservative',accent:'#273e50',secondary:'#667780',font:'Arial'},{name:'Technical',accent:'#236155',secondary:'#5c7c71',font:'Calibri'},{name:'Modern',accent:'#245c4b',secondary:'#6b7e74',font:'Arial'},{name:'Minimal',accent:'#303030',secondary:'#777777',font:'System Sans'},{name:'Research',accent:'#493b4d',secondary:'#7e6d80',font:'Georgia'},{name:'Executive',accent:'#4e4435',secondary:'#847762',font:'Times New Roman'}]

export default function StylePanel(){
  const style=useEditor(s=>s.resume!.document.style),change=useEditor(s=>s.change)
  function set<K extends keyof ResumeStyle>(key:K,value:ResumeStyle[K]){change(r=>{r.document.style[key]=value})}
  return <><p className="form-note">Make it yours. Every adjustment updates the paper immediately.</p><div className="preset-grid">{presets.map(p=><button key={p.name} onClick={()=>change(r=>{r.document.style={...r.document.style,accent:p.accent,secondary:p.secondary,font:p.font}})}><span style={{background:p.accent}}/>{p.name}</button>)}</div><label className="field"><span>Font family</span><select value={style.font} onChange={e=>set('font',e.target.value)}>{['Arial','Calibri','Georgia','Times New Roman','Verdana','System Sans'].map(font=><option key={font}>{font}</option>)}</select></label>{[{key:'font_size',label:'Font size',min:9,max:16,step:.5,unit:'pt'},{key:'line_height',label:'Line height',min:1.1,max:2,step:.05,unit:''},{key:'margin',label:'Page margin',min:8,max:32,step:1,unit:'mm'},{key:'section_spacing',label:'Section spacing',min:4,max:32,step:1,unit:'px'},{key:'bullet_spacing',label:'Bullet spacing',min:0,max:16,step:1,unit:'px'}].map(control=><label className="field range-field" key={control.key}><span>{control.label}<output>{style[control.key as keyof ResumeStyle]} {control.unit}</output></span><input aria-label={control.label} type="range" min={control.min} max={control.max} step={control.step} value={style[control.key as keyof ResumeStyle]} onChange={e=>set(control.key as 'font_size',Number(e.target.value))}/></label>)}<div className="property-subheading">COLOR PALETTE</div>{[{key:'accent',label:'Primary color'},{key:'secondary',label:'Secondary color'},{key:'text',label:'Text color'}].map(c=><label className="color-field" key={c.key}><span>{c.label}</span><input aria-label={c.label} type="color" value={style[c.key as 'accent']} onChange={e=>set(c.key as 'accent',e.target.value)}/><code>{style[c.key as 'accent']}</code></label>)}<div className="property-subheading">PAGE & DETAILS</div><label className="field"><span>Paper size</span><select value={style.page_size} onChange={e=>set('page_size',e.target.value as 'A4'|'Letter')}><option>A4</option><option value="Letter">US Letter</option></select></label><label className="field"><span>Header alignment</span><select value={style.alignment} onChange={e=>set('alignment',e.target.value as 'left'|'center')}><option value="left">Left</option><option value="center">Center</option></select></label><label className="field"><span>Heading style</span><select value={style.heading_style} onChange={e=>set('heading_style',e.target.value)}><option value="uppercase">Uppercase</option><option value="title">Title case</option><option value="small-caps">Small caps</option></select></label><label className="field"><span>Date style</span><select value={style.date_style} onChange={e=>set('date_style',e.target.value)}><option value="original">As entered</option><option value="short">Sep 2026</option><option value="long">September 2026</option></select></label><p className="form-note">System fonts only. If a font is unavailable on this computer, the system uses its local fallback. Minimum font size is 9 pt.</p></>
}
