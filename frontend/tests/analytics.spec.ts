import {test,expect} from '@playwright/test'
test('goals and live analytics',async({page,request})=>{
 await page.goto('/');await page.getByRole('button',{name:'Career Goals',exact:true}).click();await page.getByRole('button',{name:'Create career goal'}).click();const d=page.getByRole('dialog');await d.getByRole('textbox',{name:'Career goal title'}).fill('Complete portfolio case study');await d.getByRole('spinbutton',{name:'progress',exact:true}).fill('40');await d.getByRole('button',{name:'Save career goal'}).click();await expect(page.locator('.goal-card').filter({hasText:'Complete portfolio case study'})).toContainText('40');await page.getByRole('button',{name:'Analytics',exact:true}).click();const data=await(await request.get('/api/analytics')).json();await expect(page.locator('.metric-card').filter({hasText:'Applications sent'}).locator('strong')).toHaveText(String(data.sent));await page.getByRole('button',{name:'Dashboard',exact:true}).click();await expect(page.getByText('Your application pipeline')).toBeVisible();await page.screenshot({path:'test-results/dashboard-phase21.png',fullPage:true})
})
