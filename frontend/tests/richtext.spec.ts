import {test,expect} from '@playwright/test'
test('inline autosave while focused and ATS-safe rich text',async({page,request})=>{const profile=await(await request.get('/api/profile')).json();const r=await(await request.post('/api/resumes',{data:{name:'Rich text test '+Date.now(),selected_ids:profile.items.map((i:any)=>i.id)}})).json();await page.goto('/');await page.getByRole('button',{name:'Resumes',exact:true}).click();await page.getByRole('button',{name:'Open '+r.name,exact:true}).click();const name=page.getByLabel('Edit full name',{exact:true});await name.fill('Alex Inline Autosave');await expect.poll(async()=> (await(await request.get('/api/resumes/'+r.id)).json()).document.personal.full_name).toBe('Alex Inline Autosave');await expect(name).toBeFocused();await page.getByRole('button',{name:'02 Summary',exact:true}).click();const summary=page.getByRole('textbox',{name:'Professional summary',exact:true});await summary.fill('Built reliable systems');await summary.press('Control+a');await page.getByRole('button',{name:'Bold',exact:true}).click();await expect(page.locator('.paginated-document strong').filter({hasText:'Built reliable systems'})).toBeVisible();await expect.poll(async()=>JSON.stringify((await(await request.get('/api/resumes/'+r.id)).json()).document.summary_rich)).toContain('bold')})
